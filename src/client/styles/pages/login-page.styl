#login-page
  min-height 100vh
  display flex
  align-items center
  justify-content center
  background $steel
  .login-form
    width 40%
    padding 50px
    .logo
      position absolute
      top -100px
      left 20px
      height 80px
      width auto
      opacity 0.5
    .card-heading
      margin 0
